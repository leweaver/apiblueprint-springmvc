package {{ package }}.controller;

import {{ package }}.model.*;
import {{ package }}.service.{{apiSpecClassName}};
import org.springframework.web.bind.annotation.*;
import org.springframework.beans.factory.annotation.Autowired;
{{#extraImports}}
import {{.}};
{{/extraImports}}

{{> header }}
@RestController{{#metadata.API_PREFIX}}
@RequestMapping("{{{metadata.API_PREFIX}}}"){{/metadata.API_PREFIX}}
public class {{controllerClassName}} {

    private {{apiSpecClassName}} {{apiSpecName}};

    @Autowired
    public {{controllerClassName}}({{apiSpecClassName}} {{apiSpecName}}) {
        this.{{apiSpecName}} = {{apiSpecName}};
    }
    {{#resources}}
        {{#actions}}
            {{> resource}}
        {{/actions}}
    {{/resources}}

}